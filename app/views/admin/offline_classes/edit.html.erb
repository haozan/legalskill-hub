<% content_for :title, "编辑班次" %>

<div class="container-xl max-w-3xl">
  <div class="mb-6">
    <%= link_to "← 返回班次列表", admin_offline_classes_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
  <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50 mb-8">编辑班次</h1>

  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <%= render "form", offline_class: @offline_class %>
    </div>
  </div>
</div>
